LumaCycle fixed single-file build. Upload ONLY index.html to GitHub Pages if you want; all artwork is embedded directly inside the HTML, so no assets folder is required. Swipe the illustration left/right to move one day at a time.
